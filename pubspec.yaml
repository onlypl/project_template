name: project_template
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.7.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1 #网络请求
  dio_cookie_manager: ^3.2.0 #cookie管理
  cookie_jar: ^4.0.8 #cookie管理
  path_provider: ^2.1.5 #获取沙盒路径
  uuid: ^4.5.1 #生成UUID
  flutter_udid: ^4.0.0 #UDID
  device_info_plus: ^11.3.3 #设备信息
  video_player: ^2.9.5 #官方播放器插件
  chewie: ^1.11.1  #基于video_payer封装的播放器
  pull_to_refresh: ^2.0.0 #上下拉刷新 7.64587525
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  json_annotation: ^4.9.0  #创建数据模型配合json_serializable,build_runner使用
  fluttertoast: ^8.2.12      #Toast提示
  bot_toast: ^4.1.3  #Toast提示
  flutter_easyloading: ^3.0.5 # HUD
  card_swiper: ^3.0.1  #轮播图
  flutter_screenutil: ^5.9.3 #适配屏幕和字体大小的flutter插件。让你的UI在不同的屏幕尺寸上显示合理的布局
  flutter_gen_runner: ^5.10.0 #资源路径自动生成路径 dart run build_runner build
  convex_bottom_bar: ^3.2.0 #底部导航栏
  logger: ^2.5.0        #log日志打印
  connectivity_plus: ^6.1.3 #网络检测状态
  cached_network_image: ^3.4.1 #图片下载缓存库
  flutter_staggered_grid_view: ^0.7.0 #瀑布流
  status_bar_control: ^3.2.1 #状态栏控件，可让您控制 iOS 和 Android 上的状态栏颜色、样式（主题）、可见性和半透明属性。
  transparent_image: ^2.0.1 #淡入淡出图片
  webview_flutter: ^4.10.0 #webView组件
  loading_animation_widget: ^1.3.0 #全局加载动画
  shared_preferences: any #数据持久化缓存 //2.5.3
  fl_chart: 0.70.2 #图表库
  flex_color_scheme: ^8.2.0 #主题
  buttons_tabbar: ^1.3.15 #按钮tabbar
  auto_size_text: ^3.0.0 #自适应字体大小
  #tab_indicator_styler //选项卡切换指示器
  pin_code_fields: ^8.0.1 #6格验证码
  city_pickers: ^1.3.0 #省市区三级联动
  crypto: ^3.0.6 #MD5加密和SHA
  hive: ^2.2.3 #NoSQL数据库  flutter packages pub run build_runner build
  hive_flutter: ^1.1.0 #hive NoSQL数据库管理
  flutter_spinkit: ^5.2.1 #loading动画
  scrollview_observer: ^1.26.0 #监听滚动视图中正在显示的子部件 支持滚动到指定下标位置
  provider: ^6.1.4        #状态管理
  input_quantity: ^2.5.1 #输入数量 左右加减按钮
  comment_tree: ^0.3.0  #评论列表和评论回复列表二级树
  intl: ^0.20.2 #提供国际化和本地化设施，包括消息翻译、复数和性别、日期/数字格式和解析以及双向文本。
  screen_brightness: ^2.1.2 #屏幕亮度修改
  html: ^0.15.5
  introduction_screen: ^3.1.17 #引导页
  get: ^4.7.2
  awesome_notifications: ^0.10.1 #远程/本地推送通知自定义布局 Build Libraries for Distributio 要设置为NO
  web_socket_channel: ^3.0.2 #webSocket
  rxdart: ^0.28.0 #webSocket
dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.9.4 #数据模型序列化 json_annotation,build_runner使用
  build_runner: ^2.4.15 #数据模型生成


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  assets:
    - assets/images/
    - assets/images/barrage/
    - assets/images/common/
    - assets/vectors/
    - assets/fonts/
  fonts:
#    - family: tvIcon   #指定一个字体名 根据自己的需求定义
#      fonts:
#        - asset: assets/fonts/iconfont.ttf
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
          weight: 300
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 700
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
          weight: 300
        - asset: assets/fonts/Cairo-Medium.ttf
          weight: 500
        - asset: assets/fonts/Cairo-SemiBold.ttf
          weight: 900
